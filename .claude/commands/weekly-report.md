Run the onboarding weekly report. Execute the Python script and report the output file locations.

Steps:
1. Run: `cd /Users/winniesi/Downloads/onboarding-data/scripts && python3 main.py`
2. If dependency errors, run: `pip3 install -r /Users/winniesi/Downloads/onboarding-data/scripts/requirements.txt` then retry
3. Report the generated HTML and Excel file paths to the user
